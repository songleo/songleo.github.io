# LEo的网络日志

这是 `reborncodinglife.com` 的 astropaper 预览分支。

## 本地预览

```sh
pnpm install
pnpm dev
```

生产构建：

```sh
pnpm build
pnpm preview
```

jekyll 版本保留在 `main` 分支和
`jekyll-before-astro-paper-current-20260904` 标签中。
